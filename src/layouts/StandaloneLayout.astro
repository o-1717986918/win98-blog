---
import BackBadge from '../components/chrome/BackBadge.astro';
import { SITE, type ThemeId } from '../config/site';
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description?: string;
  theme?: ThemeId;
  pageKind?: 'standalone' | 'post' | 'column';
  published?: Date | undefined;
  updated?: Date | undefined;
  tags?: string[] | undefined;
  noindex?: boolean | undefined;
  image?: string | undefined;
  back?: boolean | undefined;
  schemaType?: 'WebPage' | 'CollectionPage' | 'ProfilePage' | undefined;
}
const { title, description, theme = SITE.defaultTheme, pageKind = 'standalone', published, updated, tags, noindex, image, back = true, schemaType } = Astro.props;
---

<BaseLayout title={title} description={description ?? SITE.description} theme={theme} {pageKind} {published} {updated} {tags} {noindex} {image} {schemaType}>
  {back && <BackBadge />}
  <slot />
</BaseLayout>
