# AGENTS.md

本文件约束所有在本仓库工作的自动化代理和开发者。除非用户明确覆盖，以下规则适用于整个仓库。

## 1. 先读后改

开始修改前依次阅读：

1. `README.md`
2. `docs/handover/CURRENT_STATE.md`
3. `docs/handover/PRINCIPLES.md`
4. `docs/handover/TAKEOVER_PLAN.md`
5. 与任务相关的 `docs/decisions/*.md`

`docs/source/blog-architecture.md` 是历史设计输入；遇到冲突，以已接受 ADR 和当前接手文档为准。

## 2. 仓库边界

- 不修改或删除外部源目录 `C:\Users\26532\.zcode\workspace\default`。
- `prototype/index.html` 是迁移验收基线。正式工程建立前保持其哈希不变；若用户明确要求修原型，先更新迁移清单并说明原因。
- 不把 `prototype/index.html` 的 hash 路由、`innerHTML` 视图渲染或单文件结构复制进生产代码。
- 不提交 `.env`、密钥、账号信息、构建产物、缓存或第三方生成目录。

## 3. 产品和架构契约

- 内容身份稳定：文章始终保有同域规范 URL，并进入同一份 sitemap、RSS 和搜索索引。
- 外壳采用加法组合：`BaseLayout` 只负责文档骨架；导航、页脚、目录、评论等都是显式装配的 chrome。
- 自由度渐进：默认 `full`，特殊文章先考虑 `minimal`，只有确有必要才使用 `none`。
- 静态优先：正文在构建期生成 HTML；客户端 JavaScript 必须由真实交互需求证明其必要性。
- 主题即数据：颜色、排版和空间语义由设计令牌驱动；Canvas 与 DOM 读取同一语义令牌。
- 文章共置：文章正文、私有组件、脚本和媒体放在同一文章目录；跨文章复用后再提升到公共组件。
- 不默认引入 Tailwind、整站 React/Vue、SPA 路由或运行时全局状态库。

## 4. 工作方式

- 先写或更新可验证的验收条件，再实现。
- 优先小步迁移和可回滚提交，不做无依据的大规模重写。
- 新的长期约束写成 ADR；不要只留在聊天、提交信息或代码注释里。
- 依赖采用“需要时引入”；新增包必须说明用途、替代方案和客户端体积影响。
- 修改内容 schema、路由、布局档位、主题契约或部署目标时，必须同步文档与架构测试。
- 不顺手修复与当前任务无关的问题；记录到接手计划或 issue 清单。

## 5. 验证门槛

每次提交至少运行与改动相称的检查。正式 Astro 工程建立后，最低门槛为：

```text
pnpm check
pnpm test
pnpm build
```

涉及 UI 时还要验证：

- 390 × 844 与 1280 × 720 两个基准视口；
- 键盘导航、焦点可见性、跳转链接和对话框焦点管理；
- `prefers-reduced-motion`；
- 页面无横向溢出；
- `full`、`minimal`、`none` 三档至少各一个代表页；
- 控制台无新增错误，关键页面无明显 CLS。

当前接手基线可先运行 `pnpm check`，原型预览使用 `pnpm preview:prototype`。

## 6. 完成定义

任务只有在代码、测试、文档和验证证据一致时才算完成。若受外部账号、域名、评论仓库或统计服务阻塞，应保持功能可关闭，并明确列出阻塞项，不得写入虚假占位凭据。
